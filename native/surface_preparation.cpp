// IMPLEMENTATION PENDING: intentionally excluded from active CMake targets.
#include "surface_preparation.hpp"

// Refine existing TriangleMesh/PolyhedralBRep ownership and provenance first.
// Requests group admitted input, typed policy and operation limits; do not hide untyped data.
// Share immutable geometry storage across preparation, operators and admitted charts.
// Retain the convex fan as a restricted fast path; qualify concave/warped-face behavior
// against integration/mesh_healing_extension/mesh_healing_engine.py.
// Return sparse operators; preserve Python HarmonicSystem constraints, SciPy solve,
// factorization reuse, residual policy and fallback. Local chart validity is not global
// injectivity. Qualify float64 and checked index support before NumPy integration.

// No placeholder success, throwing stub, or algorithm definition is provided.
